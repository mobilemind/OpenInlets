# CodeQL Configuration
# Exclude bookmarklet ("web") files from JavaScript analysis
---

name: "CodeQL Config"

paths-ignore:
  - web/*.js  # Bookmarklets (JavaScript URIs, not true JavaScript files)
