# CodeQL Configuration
# Exclude bookmarklet files from JavaScript analysis
---

name: "CodeQL Config"

paths-ignore:
  - dist/*.bookmarklet  # Bookmarklet files (javascript: URIs, not JavaScript files)
