# NPM Security Configuration for OpenInlets

# Enable audit checks during install
audit=true
audit-level=moderate

# Require package-lock.json and fail if out of sync
package-lock=true

# Don't automatically save to package.json when installing
save-exact=true

# Prevent legacy peer dependency issues
legacy-peer-deps=false
